import {StyleSheet} from 'react-native';

function useStyleCategoriesSection() {
  const styles = StyleSheet.create({
    section: {flex: 1},
  });

  return styles;
}

export default useStyleCategoriesSection;
